# K-Buddy API — Python 3.12
# planner 는 3.11 을 명시했으나, 로컬(3.12.3)과 Docker 핀을 일치시켜
# "로컬에선 됐는데 배포에서 안 되는" 사고를 제거한다. 휠 호환은 dev-log 2026-08-11 참조.

# ── 웹 ───────────────────────────────────────────────────────────────
fastapi~=0.115
uvicorn[standard]~=0.32
pydantic~=2.9
pydantic-settings~=2.6
slowapi~=0.1.9              # IP당 분 20회 (planner §9.1)
httpx~=0.27

# ── LLM ──────────────────────────────────────────────────────────────
anthropic~=0.69             # 생성(sonnet-5) + 질의 정규화(haiku-4.5)

# ── 검색 ─────────────────────────────────────────────────────────────
chromadb~=0.5.20            # 로컬 persistent, ~1,200 청크에 별도 서버 불필요
rank-bm25~=0.2.2
kiwipiepy~=0.18             # 한국어 형태소 — 공백 분리 시 "한도제한계좌" 매칭 실패
fastembed~=0.4              # BGE-m3 ONNX. 문서·쿼리 임베딩에 같은 모델/정밀도 사용

# ── 코퍼스 수집 (corpus/scripts) ─────────────────────────────────────
trafilatura~=1.12           # HTML 본문 추출
pdfplumber~=0.11            # PDF + extract_table()
python-frontmatter~=1.1
pyyaml~=6.0

# ── 문서 생성 ────────────────────────────────────────────────────────
# M1 에서 사용하지 않지만 지금 고정한다. Docker 시스템 의존성(libpango/libcairo)을
# 6주 차에 처음 마주치면 늦는다 — planner §15.4 가 지적한 최빈 배포 사고.
weasyprint~=63.0

# ── 로컬 생성 모델 (LLM_BACKEND=local, ADR-004) ──────────────────────
# 개발은 mps(Apple Silicon), 배포는 cuda(AWS g5/g6) — **같은 코드 경로**로
# 양쪽을 돌린다. vLLM 은 처리량이 좋지만 MPS 를 지원하지 않아 개발과 배포가
# 갈라지므로 쓰지 않는다(단일 사용자 데모라 처리량이 병목이 아니다).
torch~=2.13
# ★ 5.x 가 **필요하다.** 4.57 은 `qwen3_5`·`gemma4` 아키텍처를 모른다
#   ("Transformers does not recognize this architecture"). 후보 3종 중 2종이
#   그 계열이라 4.x 로는 재 볼 수조차 없다 (실측 2026-08-17).
transformers~=5.15
accelerate~=1.14
# 구조화 출력 강제. 문법 제약이 없으면 citations·numbers_used 가 깨지고
# 인용 검사·숫자 대조가 통째로 무력해진다 — 이 백엔드의 성패가 여기 걸린다.
xgrammar~=0.2

# ⚠ transformers 가 tokenizers 를 올리면서 chromadb 0.5.23 의 선언
#   (`tokenizers<=0.20.3`)과 충돌한다는 pip 경고가 뜬다. **실동작은 정상**이다 —
#   검색 회귀(Recall@5 93.9% / 과잉폴백 6.1%)가 설치 전후로 동일했다.
#   chromadb 쪽 선언이 보수적인 것이므로 버전을 내리지 않는다.
#
# ⚠ xgrammar 0.2.4 도 `transformers<5` 를 선언해 같은 경고가 뜬다. 같은 판단을
#   적용하되 **더 조심해서 본다** — 문법 제약은 이 백엔드의 성패가 걸린 지점이라
#   "임포트가 되니까 괜찮다"로 끝낼 수 없다. 현재 확인된 것은 임포트와
#   `xgrammar.contrib.hf.LogitsProcessor` 로딩까지이고, **실제 문법 제약 생성은
#   가중치를 받아 `scripts/verify_local_model.py` 로 확인해야 한다.**

# ── 리포트 그림 (eval/report_charts.py) ──────────────────────────────
# 리포트 JSON 은 튜닝의 정량 증거이지만 사람이 읽는 형태가 아니다. planner §13 이
# 이 파일들을 "발표 자료의 튜닝 과정 슬라이드 원자료"로 못박아 두었으므로
# 기획서에 붙일 그림이 필요하다. kaleido 는 PNG 내보내기용(HTML 만 쓸 거면 불필요).
plotly~=5.24
kaleido==0.2.1              # 0.4 계열은 chrome 을 따로 요구한다 — 핀을 고정한다

# ── 테스트 ───────────────────────────────────────────────────────────
pytest~=8.3
pytest-asyncio~=0.24
